// A pure function is a function in JavaScript which takes the same input and gives the same output
// It doesn't deppends on any external factors

function addPure(x, a){
    return a+x;
}

console.log(addPure(10,20));
console.log(addPure(10,20));
console.log(addPure(10,20));