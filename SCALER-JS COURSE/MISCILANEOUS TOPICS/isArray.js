//Array.isArray() method is used to check whether the given variable is Array or not
//If array returns true
//else returns false

let arr = [1, 2, 3, 4, 5, 6];
console.log(Array.isArray(arr));

let myObj = {
    name : 'Rubin',
    age : 20
};

console.log(Array.isArray(myObj));