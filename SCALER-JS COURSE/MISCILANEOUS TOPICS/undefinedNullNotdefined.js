//undefined - using the variable(printing it) before intializing the value to it

let a;
console.log(a);

//null - an explicit value that represents that there is no object value

let b = null;
console.log(b);

//Not defined - error that is throwed when you use an undeclared variable

console.log(c);